/target
# Cargo.lock is committed on purpose: this crate is linked into a Service that
# is published from a pinned commit, and without the lock file the same commit
# can resolve different dependency versions and produce different bytes.
